#include <iostream>
int main() 
{
    int z = 1;
    double x,y;
    std::cout << "Сколько отмыл Иван Иванович в первый день?\n";
    std::cin >> x;
    std::cout << "Какая сумма заинтересует ФНС?\n";
    std::cin >> y;
    while (x<=y)
    {
        x = x+x*0.1;
        ++z;
    }
    std::cout << z;
}
